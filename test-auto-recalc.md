# 自动重新计算功能测试指南

## 功能描述
已实现市价单未锁定价格时每两秒自动重新计算一次的功能。如果价格锁定，则静态显示结果。

## 实现的功能

### 1. 自动重新计算逻辑 (ResultCard.tsx)
- **条件**: 
  - 订单类型为 `MARKET` (市价单)
  - 价格未锁定 (`!isPriceLocked`)
  - 已有计算结果 (`result`)
  - 有市场元数据 (`marketMeta`)
  - 有必要的表单数据 (交易所、交易对、方向等)

- **频率**: 每2秒执行一次
- **操作**: 
  - 获取当前市场价格
  - 更新实时价格显示
  - 使用新价格重新计算仓位结果
  - 更新所有相关数据（止损价格、预期盈亏等）

### 2. 价格锁定机制 (CalculatorForm.tsx + ResultCard.tsx)
- **锁定状态**: 当用户点击"锁定"按钮时，价格被锁定
- **锁定效果**: 
  - 停止自动重新计算
  - 计算结果基于锁定价格保持静态
  - 显示"已锁定"状态指示器

- **解锁效果**:
  - 恢复自动重新计算
  - 显示"每2秒更新"状态指示器
  - 结果根据实时价格动态更新

### 3. 视觉指示器
- **未锁定状态**: 🔄 "每2秒更新" (绿色，有动画效果)
- **锁定状态**: 🔒 "已锁定" (黄色)
- **计算结果区域**: 在未锁定时有绿色边框闪烁效果

## 代码修改要点

### ResultCard.tsx
1. 添加了 `setRealTimePrice` 和 `setLastPriceUpdate` 到 store 导入
2. 优化了自动重新计算的 useEffect
3. 使用正确的 `calculatePosition` 输入格式
4. 更新了依赖数组，包含所有相关表单字段
5. 改进了状态指示器文本

### CalculatorForm.tsx  
1. 将实时价格更新间隔从3秒改为2秒，与自动重新计算同步
2. 添加了注释说明价格更新和结果重新计算的分工

## 测试步骤

### 准备工作
1. 启动应用: `npm run dev`
2. 确保网络连接正常（非离线模式）
3. 选择交易所（如 BINANCE）和交易对（如 BTCUSDT）

### 测试自动重新计算
1. **设置市价单**:
   - 订单类型选择 "市价单 (MARKET)"
   - 填写必要参数（风险金额、止损模式等）
   - 点击"计算仓位"

2. **观察自动更新**:
   - 计算结果显示后，应该看到"🔄 每2秒更新"指示器
   - 入场价格应该每2秒更新一次
   - 相关计算结果（数量、止损价格、风险等）应该跟随价格变化

3. **验证更新频率**:
   - 观察价格变化时间戳
   - 确认间隔约为2秒

### 测试价格锁定
1. **锁定价格**:
   - 在市价单计算结果显示时，点击入场价格旁的"🔓 锁定"按钮
   - 应该看到按钮变为"🔒 已锁定"
   - 状态指示器变为"🔒 已锁定"（黄色）

2. **验证静态显示**:
   - 等待超过2秒，确认价格不再更新
   - 计算结果应该保持静态
   - 绿色闪烁效果应该消失

3. **解锁价格**:
   - 点击"🔒 已锁定"按钮解锁
   - 应该恢复"🔄 每2秒更新"状态
   - 自动更新应该立即恢复

### 测试边界情况
1. **切换订单类型**:
   - 从市价单切换到限价单，自动更新应该停止
   - 从限价单切换回市价单，自动更新应该恢复

2. **离线模式**:
   - 在离线模式下，市价单应该不可用
   - 自动更新功能应该不激活

3. **网络错误**:
   - 模拟网络错误，自动更新应该优雅地处理失败
   - 不应该影响现有的计算结果

## 预期行为

### 正常运行时
- 市价单未锁定：每2秒自动获取新价格并重新计算
- 市价单已锁定：显示静态结果，不更新
- 限价单：不进行自动更新（正常行为）

### 性能影响
- 网络请求：每2秒一次 getCurrentPrice API 调用
- 计算负载：每2秒一次 calculatePosition 调用
- UI 更新：实时更新结果显示

## 注意事项
1. 只有市价单才有自动重新计算功能
2. 必须先完成一次计算才会开始自动更新
3. 锁定/解锁状态影响所有相关计算，包括止损、止盈价格
4. 自动更新使用与手动计算相同的逻辑和参数
