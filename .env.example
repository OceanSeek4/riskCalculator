# Development server port configuration
# Default port is 1420, change if occupied
VITE_DEV_PORT=1420

# HMR port (usually DEV_PORT + 1)
VITE_HMR_PORT=1421

# Tauri dev host (leave empty for localhost)
TAURI_DEV_HOST=